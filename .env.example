# URL base del backend (sin barra final).
# - En desarrollo (sin esta variable) el front cae al fallback http://127.0.0.1:5001
# - En producción Vite la inyecta al hacer build a través de .env.production
# Ver src/config/leadWebhook.js
VITE_API_BASE=
